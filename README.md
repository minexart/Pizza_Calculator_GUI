# Pizza_Calculator_GUI
This program is a Swing-based “Pizza Calculator” Graphical User Interface(GUI) that allows the user to enter the number of people and select a hunger level (light, medium or ravenous). It then calculates how many pizzas are needed for each selected pizza type based on the number of slices per pizza and displays the results in a table. 
